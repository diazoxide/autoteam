workers:
  - name: "dev1"
    prompt: "Developer"

settings:
  flow:
    - name: collector
      type: gemini
      input: "Collect tasks"
    - name: executor
      type: claude
      depends_on: [collector]
      input: "Execute tasks"