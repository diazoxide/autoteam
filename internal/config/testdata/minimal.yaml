agents:
  - name: "dev1"
    prompt: "Developer"

settings:
  flow:
    - name: collector
      type: gemini
      prompt: "Collect tasks"
    - name: executor
      type: claude
      depends_on: [collector]
      prompt: "Execute tasks"